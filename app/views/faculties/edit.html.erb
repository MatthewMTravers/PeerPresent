<head>
  <title>Edit Faculty</title>
  <%= stylesheet_link_tag 'session', media: 'all' %>
  <%= stylesheet_link_tag 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css' %>
</head>

<body>
  <div class="container">
    <div class="wrapper">
      <div class="title"><span>Edit Faculty</span></div>

      <%= form_with(model: @current_faculty, url: faculty_path(@current_faculty), local: true, method: :patch) do |form| %>
        <div class="row">
          <i class="fas fa-user"></i>
          <%= form.text_field :fname, class: 'new-user', value: @current_faculty.fname %>
        </div>

        <div class="row">
          <i class="fas fa-user"></i>
          <%= form.text_field :lname, value: @current_faculty.lname %>
        </div>

        <div class="row">
          <i class="fas fa-envelope"></i>
          <%= form.text_field :email, class: 'new-user', value: @current_faculty.email %>
        </div>

        <div class="row">
          <i class="fas fa-lock"></i>
          <%= form.password_field :password, placeholder: 'New Password' %>
        </div>

        <div class="row">
          <i class="fas fa-lock"></i>
          <%= form.password_field :password_confirmation, placeholder: 'Confirm New Password' %>
        </div>

        <div class="row button">
          <%= form.submit 'Save Changes' %>
        </div>

        <div class="signup-link">
          <%= link_to 'Back to Faculty Home', faculty_home_path %>
        </div>
      <% end %> 
    </div>
  </div>
</body>
